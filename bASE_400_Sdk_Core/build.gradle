apply plugin: 'com.android.library'

android {
    compileSdkVersion 22
    buildToolsVersion "25.0.0"

    defaultConfig {
        minSdkVersion 9
        targetSdkVersion 22
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-project.txt'
        }
    }

    lintOptions {
        abortOnError false
    }

}

dependencies {
    compile files('libs/org.apache.http.legacy.jar')
    compile 'com.squareup.retrofit2:converter-gson:2.0.2'
}

//  没有混淆包
task makeJar(type: Copy) {
    delete 'build/outputs/jar/zfSdkCore.jar'
    from('build/intermediates/bundles/default/')
    into('build/outputs/jar/')
    include('classes.jar')
    rename ('classes.jar', 'zfSdkCore.jar')
}

//  混淆包混淆配置文件
//task makeJar(type: proguard.gradle.ProGuardTask,dependsOn:"build") {
//    delete'build/outputs/jar/zfSdkCore.jar'//删除之前编译混淆jar包
//    injars'build/intermediates/bundles/default/classes.jar'//项目编译但未混淆的jar
//    outjars'build/outputs/jar/zfSdkCore.jar'//混淆后的jar路径
//    configuration'proguard-project.txt'// 混淆配置文件
//}



makeJar.dependsOn(build)
